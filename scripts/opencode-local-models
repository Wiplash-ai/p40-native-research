#!/usr/bin/env bash
set -euo pipefail

# Compatibility alias. The local-model provider and Better Hashline are in
# OpenCode's global configuration, so this invokes normal OpenCode without a
# separate profile.
exec opencode "$@"
