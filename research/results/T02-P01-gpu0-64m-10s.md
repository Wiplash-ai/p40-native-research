# T02 P01 — GPU0 64 MiB copy canary, ten seconds

Date: 2026-09-08. Status: **pass**.

This final P01 duration gate is a ten-second, power-limited thermal canary,
not a peak-bandwidth or model-inference result.

| Field | Result |
|---|---|
| Primitive | P01 contiguous FP32 device copy |
| GPU | Tesla P40 GPU0 |
| Array size | 64 MiB input + 64 MiB output |
| Power policy | 125 W during the canary; restored to 250 W |
| Timed work | 21,664 copies / 10,005.1 ms |
| Algorithmic bandwidth | 290.620 GB/s (read + write accounting) |
| Correctness | exact final-element readback passed |
| Temperature | 35°C start; 41°C peak during work; 36°C after cooldown |
| VRAM after work | 0 MiB on both P40s |
| FAN1–FAN8 | 2000–2100 RPM throughout |
| New critical SEL | none |

Raw durable server record:
[`raw/T02-P01-gpu0-64m-10s-37f975ae.json`](raw/T02-P01-gpu0-64m-10s-37f975ae.json)
(`SHA-256 974bc7ea3dd3fab7289ed336d11a993d278dde77897736628cdecf184d13e7ef`).

## Interpretation

The three duration gates are stable: 290.691 GB/s (1 s), 290.638 GB/s (5 s),
and 290.620 GB/s (10 s), a 0.02% range. GPU0's sampled peak rose only to 41°C
and returned to 36°C during the hold. This qualifies the bounded 125 W P01
path for the next primitive; it does not qualify an uncapped workload, a
model-resident thermal state, or inference performance.

The local display client timed out at 380 seconds before the server completed
its final BMC checks, but the independent durable result was `pass`. The client
timeout has been increased to 480 seconds so that future complete guarded runs
are not reported as local failures.

## Next gate

Run a one-second P03 INT8 DP4A GEMV canary under the same GPU0/125 W/complete
cooldown policy. Its exact host `int64` reference check must pass before any
larger integer-kernel experiments.
