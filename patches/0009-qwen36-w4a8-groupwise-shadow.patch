--- a/c/backend_cuda.cu
+++ b/c/backend_cuda.cu
@@ -856,6 +856,23 @@
     if(threadIdx.x<32){m=threadIdx.x<8?p[threadIdx.x]:0;for(int n=16;n;n>>=1)m=fmaxf(m,__shfl_down_sync(0xffffffff,m,n));if(!threadIdx.x)sc[r]=m>0?m/127.f:1.f;}__syncthreads();
     float inv=1.f/sc[r];int8_t *dst=q+(size_t)r*width;for(int i=threadIdx.x;i<width;i+=blockDim.x){int v=__float2int_rn(src[i]*inv);dst[i]=(int8_t)max(-127,min(127,v));}
 }
+
+/* Shadow-only groupwise activation quantization. Group size is deliberately
+ * fixed and divisible by the DP4A pack width; the group scale layout is
+ * [row][ceil(width/256)]. */
+#define DP8_GROUPWISE_WIDTH 256
+__global__ static void dp8_quant_rows_groupwise(int8_t *q,float *sc,const float *x,
+                                                 int rows,int width){
+    int r=blockIdx.x,g=blockIdx.y;if(r>=rows)return;
+    int begin=g*DP8_GROUPWISE_WIDTH,end=min(width,begin+DP8_GROUPWISE_WIDTH);
+    __shared__ float p[8];float m=0;const float *src=x+(size_t)r*width;
+    for(int i=begin+threadIdx.x;i<end;i+=blockDim.x)m=fmaxf(m,fabsf(src[i]));
+    for(int n=16;n;n>>=1)m=fmaxf(m,__shfl_down_sync(0xffffffff,m,n));
+    if(!(threadIdx.x&31))p[threadIdx.x>>5]=m;__syncthreads();
+    if(threadIdx.x<32){m=threadIdx.x<8?p[threadIdx.x]:0;for(int n=16;n;n>>=1)m=fmaxf(m,__shfl_down_sync(0xffffffff,m,n));if(!threadIdx.x)sc[(size_t)r*((width+DP8_GROUPWISE_WIDTH-1)/DP8_GROUPWISE_WIDTH)+g]=m>0?m/127.f:1.f;}__syncthreads();
+    float inv=1.f/sc[(size_t)r*((width+DP8_GROUPWISE_WIDTH-1)/DP8_GROUPWISE_WIDTH)+g];int8_t *dst=q+(size_t)r*width;
+    for(int i=begin+threadIdx.x;i<end;i+=blockDim.x){int v=__float2int_rn(src[i]*inv);dst[i]=(int8_t)max(-127,min(127,v));}
+}
 __global__ static void grouped_hidden_w4a8_shadow(float *gate,const int8_t *q,const float *sc,
         const GroupDesc *desc,int I,int D){
     int c=blockIdx.z,s=blockIdx.y,warp=threadIdx.x>>5,lane=threadIdx.x&31,o=blockIdx.x*8+warp;
@@ -873,6 +890,37 @@
 #pragma unroll
     for(int i=lane*4;i<I;i+=128){uint16_t p=(uint16_t)w[i/2]|((uint16_t)w[i/2+1]<<8);a=__dp4a(*reinterpret_cast<const int*>(x+i),dp8_w4x4(p),a);}for(int n=16;n;n>>=1)a+=__shfl_down_sync(0xffffffff,a,n);if(!lane)y[(size_t)r*D+o]=(float)a*sc[r]*d.ds[o];
 }
+
+/* Keep packed W4 weights and DP4A arithmetic. Only the activation scale is
+ * per 256-value group, so each integer partial is rescaled before accumulation. */
+__global__ static void grouped_hidden_w4a8_groupwise_shadow(float *gate,const int8_t *q,
+        const float *sc,const GroupDesc *desc,int I,int D){
+    int c=blockIdx.z,s=blockIdx.y,warp=threadIdx.x>>5,lane=threadIdx.x&31,o=blockIdx.x*8+warp;
+    GroupDesc d=desc[c];if(s>=d.rows||o>=I)return;int r=d.offset+s;
+    const uint8_t *gr=(const uint8_t*)d.g+(size_t)o*((D+1)/2),*ur=(const uint8_t*)d.u+(size_t)o*((D+1)/2);
+    const int8_t *x=q+(size_t)r*D;int ng=(D+DP8_GROUPWISE_WIDTH-1)/DP8_GROUPWISE_WIDTH;float g=0,u=0;
+    for(int group=0;group<ng;group++){
+        int ga=0,ua=0,begin=group*DP8_GROUPWISE_WIDTH,end=min(D,begin+DP8_GROUPWISE_WIDTH);
+        for(int i=begin+lane*4;i+3<end;i+=128){uint16_t gp=(uint16_t)gr[i/2]|((uint16_t)gr[i/2+1]<<8);uint16_t up=(uint16_t)ur[i/2]|((uint16_t)ur[i/2+1]<<8);int xv=*reinterpret_cast<const int*>(x+i);ga=__dp4a(xv,dp8_w4x4(gp),ga);ua=__dp4a(xv,dp8_w4x4(up),ua);}
+        for(int n=16;n;n>>=1){ga+=__shfl_down_sync(0xffffffff,ga,n);ua+=__shfl_down_sync(0xffffffff,ua,n);}
+        if(!lane){float a=sc[(size_t)r*ng+group];g+=(float)ga*a*d.gs[o];u+=(float)ua*a*d.us[o];}
+    }
+    if(!lane)gate[(size_t)r*I+o]=(g/(1.f+expf(-g)))*u;
+}
+__global__ static void grouped_down_w4a8_groupwise_shadow(float *y,const int8_t *q,
+        const float *sc,const GroupDesc *desc,int D,int I){
+    int c=blockIdx.z,s=blockIdx.y,warp=threadIdx.x>>5,lane=threadIdx.x&31,o=blockIdx.x*8+warp;
+    GroupDesc d=desc[c];if(s>=d.rows||o>=D)return;int r=d.offset+s;
+    const uint8_t *w=(const uint8_t*)d.d+(size_t)o*((I+1)/2);const int8_t *x=q+(size_t)r*I;
+    int ng=(I+DP8_GROUPWISE_WIDTH-1)/DP8_GROUPWISE_WIDTH;float yv=0;
+    for(int group=0;group<ng;group++){
+        int a=0,begin=group*DP8_GROUPWISE_WIDTH,end=min(I,begin+DP8_GROUPWISE_WIDTH);
+        for(int i=begin+lane*4;i+3<end;i+=128){uint16_t p=(uint16_t)w[i/2]|((uint16_t)w[i/2+1]<<8);a=__dp4a(*reinterpret_cast<const int*>(x+i),dp8_w4x4(p),a);}
+        for(int n=16;n;n>>=1)a+=__shfl_down_sync(0xffffffff,a,n);
+        if(!lane)yv+=(float)a*sc[(size_t)r*ng+group]*d.ds[o];
+    }
+    if(!lane)y[(size_t)r*D+o]=yv;
+}
 static int dp8_shadow_launch(DeviceContext *ctx,GroupDesc *dev,int total,int D,int I,int max_rows,int count){
     size_t xb=(size_t)total*D*sizeof(float),qb=(size_t)total*(D>I?D:I);
     if(!reserve(&ctx->dp8_y,&ctx->dp8_y_cap,xb)||!reserve_bytes((void**)&ctx->dp8_q,&ctx->dp8_q_cap,qb)||!reserve(&ctx->dp8_scale,&ctx->dp8_scale_cap,(size_t)total*sizeof(float))||!reserve_pinned(&ctx->host_dp8_y,&ctx->host_dp8_y_cap,xb))return 0;
@@ -883,6 +931,17 @@
     if(!cuda_ok(cudaGetLastError(),"W4A8 DP4A shadow launch")||!cuda_ok(cudaMemcpyAsync(ctx->host_dp8_y,ctx->dp8_y,xb,cudaMemcpyDeviceToHost,ctx->stream),"W4A8 DP4A shadow download"))return 0;
     ctx->dp8_shadow_pending=1;ctx->dp8_shadow_rows=total;ctx->dp8_shadow_width=D;return 1;
 }
+static int dp8_groupwise_shadow_launch(DeviceContext *ctx,GroupDesc *dev,int total,int D,int I,int max_rows,int count){
+    size_t xb=(size_t)total*D*sizeof(float),qb=(size_t)total*(D>I?D:I);
+    size_t sb=(size_t)total*((D+DP8_GROUPWISE_WIDTH-1)/DP8_GROUPWISE_WIDTH)*sizeof(float);
+    if(!reserve(&ctx->dp8_y,&ctx->dp8_y_cap,xb)||!reserve_bytes((void**)&ctx->dp8_q,&ctx->dp8_q_cap,qb)||!reserve(&ctx->dp8_scale,&ctx->dp8_scale_cap,sb)||!reserve_pinned(&ctx->host_dp8_y,&ctx->host_dp8_y_cap,xb))return 0;
+    dp8_quant_rows_groupwise<<<dim3((unsigned)total,(unsigned)((D+DP8_GROUPWISE_WIDTH-1)/DP8_GROUPWISE_WIDTH)),256,0,ctx->stream>>>(ctx->dp8_q,ctx->dp8_scale,ctx->x,total,D);
+    grouped_hidden_w4a8_groupwise_shadow<<<dim3((I+7)/8,max_rows,count),256,0,ctx->stream>>>(ctx->gate,ctx->dp8_q,ctx->dp8_scale,dev,I,D);
+    dp8_quant_rows_groupwise<<<dim3((unsigned)total,(unsigned)((I+DP8_GROUPWISE_WIDTH-1)/DP8_GROUPWISE_WIDTH)),256,0,ctx->stream>>>(ctx->dp8_q,ctx->dp8_scale,ctx->gate,total,I);
+    grouped_down_w4a8_groupwise_shadow<<<dim3((D+7)/8,max_rows,count),256,0,ctx->stream>>>(ctx->dp8_y,ctx->dp8_q,ctx->dp8_scale,dev,D,I);
+    if(!cuda_ok(cudaGetLastError(),"W4A8 groupwise shadow launch")||!cuda_ok(cudaMemcpyAsync(ctx->host_dp8_y,ctx->dp8_y,xb,cudaMemcpyDeviceToHost,ctx->stream),"W4A8 groupwise shadow download"))return 0;
+    ctx->dp8_shadow_pending=1;ctx->dp8_shadow_rows=total;ctx->dp8_shadow_width=D;return 1;
+}
 
 __global__ static void grouped_down_w4(float *y,const float *x,const GroupDesc *desc,int D,int I){
     int o=blockIdx.x,s=blockIdx.y,c=blockIdx.z;GroupDesc d=desc[c];if(s>=d.rows)return;
@@ -2171,6 +2230,13 @@
          * sync dispatch, silu fused in the dual epilogue. */
         f8_group_launch(ctx,(GroupDesc*)ctx->group_desc,I,D,max_rows,count);
     }else if(all_s4&&ctx->compute_major==6&&ctx->compute_minor>=1&&getenv("COLI_CUDA_W4A8_DP4A")&&
+             !strcmp(getenv("COLI_CUDA_W4A8_DP4A"),"groupwise-shadow")){
+        GroupDesc *dev=(GroupDesc*)ctx->group_desc;
+        if(!dp8_groupwise_shadow_launch(ctx,dev,total,D,I,max_rows,count))return 0;
+        dim3 hg((unsigned)I,(unsigned)max_rows,(unsigned)count),og((unsigned)D,(unsigned)max_rows,(unsigned)count);
+        grouped_hidden_w4_dual<<<hg,256,0,ctx->stream>>>(ctx->gate,ctx->up,ctx->x,dev,I,D);
+        grouped_down_w4<<<og,256,0,ctx->stream>>>(ctx->y,ctx->gate,dev,D,I);
+    }else if(all_s4&&ctx->compute_major==6&&ctx->compute_minor>=1&&getenv("COLI_CUDA_W4A8_DP4A")&&
              !strcmp(getenv("COLI_CUDA_W4A8_DP4A"),"shadow")){
         GroupDesc *dev=(GroupDesc*)ctx->group_desc;
         if(!dp8_shadow_launch(ctx,dev,total,D,I,max_rows,count))return 0;
@@ -2244,7 +2310,8 @@
     if(ctx->dp8_shadow_pending){
         double se=0,sr=0,dot=0,sa=0,sb=0,ma=0;int finite=1;size_t n=(size_t)ctx->dp8_shadow_rows*ctx->dp8_shadow_width;
         for(size_t i=0;i<n;i++){double a=ctx->host_dp8_y[i],b=ctx->host_y[i],d=a-b;if(!std::isfinite(a)||!std::isfinite(b))finite=0;se+=d*d;sr+=b*b;dot+=a*b;sa+=a*a;sb+=b*b;if(fabs(d)>ma)ma=fabs(d);}
-        std::fprintf(stderr,"[w4a8-shadow] rows=%d width=%d rel_l2=%.8g max_abs=%.8g cosine=%.8g finite=%d\n",ctx->dp8_shadow_rows,ctx->dp8_shadow_width,sqrt(se/(sr>1e-30?sr:1e-30)),ma,dot/(sqrt(sa*sb)>1e-30?sqrt(sa*sb):1e-30),finite);ctx->dp8_shadow_pending=0;
+        const char *dp8_mode=getenv("COLI_CUDA_W4A8_DP4A");const char *tag=dp8_mode&&!strcmp(dp8_mode,"groupwise-shadow")?"w4a8-groupwise-shadow":"w4a8-shadow";
+        std::fprintf(stderr,"[%s] rows=%d width=%d rel_l2=%.8g max_abs=%.8g cosine=%.8g finite=%d\n",tag,ctx->dp8_shadow_rows,ctx->dp8_shadow_width,sqrt(se/(sr>1e-30?sr:1e-30)),ma,dot/(sqrt(sa*sb)>1e-30?sqrt(sa*sb):1e-30),finite);ctx->dp8_shadow_pending=0;
     }
     return ctx->host_y;
 }
